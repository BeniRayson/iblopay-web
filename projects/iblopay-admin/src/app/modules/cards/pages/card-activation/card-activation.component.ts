import { Component } from "@angular/core";

// Stub page — scope for this page (fields, workflow) was not defined yet.
// Wired into routing so the module compiles and the nav entry works;
// flesh out once requirements for activation are ready.
@Component({
  selector: "app-card-activation",
  templateUrl: "./card-activation.component.html",
  styleUrls: ["./card-activation.component.scss"]
})
export class CardActivationComponent {}
