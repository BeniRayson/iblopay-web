import { Component } from "@angular/core";

// Stub page — scope for this page (fields, workflow) was not defined yet.
// Wired into routing so the module compiles and the nav entry works;
// flesh out once requirements for stock are ready.
@Component({
  selector: "app-card-stock",
  templateUrl: "./card-stock.component.html",
  styleUrls: ["./card-stock.component.scss"]
})
export class CardStockComponent {}
