import { Component } from "@angular/core";

// Stub page — scope for this page (fields, workflow) was not defined yet.
// Wired into routing so the module compiles and the nav entry works;
// flesh out once requirements for distribution are ready.
@Component({
  selector: "app-card-distribution",
  templateUrl: "./card-distribution.component.html",
  styleUrls: ["./card-distribution.component.scss"]
})
export class CardDistributionComponent {}
